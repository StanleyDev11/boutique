spring:
  application:
    name: boutique
  
  # --- Configuration de la base de données (MySQL Active) ---
  datasource:
    url: jdbc:mysql://192.168.1.94:3306/Db_Tennis?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: ace3i
    password: 'africa@2025'
    driver-class-name: com.mysql.cj.jdbc.Driver

  # --- Configuration JPA ---
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

# --- Configuration pour PostgreSQL (Inactive) ---
#  datasource:
#    url: jdbc:postgresql://localhost:5432/boutiquedb
#    username: postgres
#    password: ''
#    driver-class-name: org.postgresql.Driver
#  jpa:
#    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080